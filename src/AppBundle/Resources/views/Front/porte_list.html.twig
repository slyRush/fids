{% extends 'AppBundle:Front:base_display.html.twig' %}
{% block mostviewbody %}
    <section class="display_content">
        <div class="departures">
            <h1 align="center" style="color: #fff">Liste Porte</h1>   
        </div>
        <table class="table table-striped table-inverse">
            <thead>
                <tr>
                    <th class="head_tabs">{{'Porte N°' | trans}}</th>
                    <th class="head_tabs">{{'Compagnie' | trans}}</th>
                    <th class="head_tabs">{{'Numéro de vol' | trans}}</th>
                    <th class="head_tabs">{{'Destination' | trans}}</th>
                    <th class="head_tabs">{{'Programmé' | trans}}</th>
                    <th class="head_tabs">{{'Statut' | trans}}</th>
                    <th class="head_tabs"></div>
                </tr>
                
            </thead>
            <tbody>
            {% for flight in checkInList %}
                <tr>
                    {% if flight['type'] == "depart" %}
                        <td class="content_tabs">{{flight['num_porte']}}</td>
                        <td class="content_tabs">
                            {% if flight['logo'] != null %}
                                <img style="width: 50px; height: 50px;" src="{{ asset(flight['logo'])}}">
                            {% endif %}
                        </td>
                        <td class="content_tabs">{{flight['nom_vol']}}</td>
                        <td class="content_tabs">
                            {% if flight['type'] == "depart" %}
                                {{flight['destination']}}
                            {% else %}    
                                {{flight['depart']}}
                            {% endif %}
                        </td>
                        <td class="content_tabs">
                            {% if flight['type'] == "depart" %}
                                {{flight['heureDepart']|date('H:i')}}
                            {% else %}    
                                {{flight['heureArrivee']|date('H:i')}}
                            {% endif %}
                        </td>
                        <td class="content_tabs">
                            {% set status = flight['statut'] %}
                            {% if status == "ontime" %}
                                En cours
                            {% elseif status == "departed" %}    
                                Parti
                            {% elseif status == "landed" %} 
                                A atteri
                            {% endif %}    
                        </td> 
                        <td class="content_tabs">
                            <a href="{{path('fids_porte_number', {'id' : flight.id})}}" >
                                    lien
                            </a>
                        </td> 
                    {% endif %}
                </tr>
                {% if flight['type'] == "depart" %}
                    <div class="border_final_raw"></div>
                {% endif %}                
            {% endfor%}
            </tbody>
                
        </table>
    </section>
{% endblock %}